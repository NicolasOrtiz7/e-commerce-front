export class Tarjeta {

    // Esta clase no existe en el back end, 
    // solo la uso para mostrar los datos en el formulario de finalizar-compra

    titular:string;
    tipo_documento:string;
    documento_titular:string;
    numero_tarjeta:string;
    vencimiento:string;
    cvv:number;

}
